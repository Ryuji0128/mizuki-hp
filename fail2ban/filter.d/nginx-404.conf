# fail2ban filter: 大量の404リクエストをブロック
# /etc/fail2ban/filter.d/nginx-404.conf にコピー

[Definition]
failregex = ^<HOST> - .* "(GET|POST|HEAD).*" 404
ignoreregex = \.(css|js|ico|png|jpg|jpeg|gif|svg|woff2?)
